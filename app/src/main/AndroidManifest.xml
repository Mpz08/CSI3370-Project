<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeDock">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RecipeDock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.platemate.LoginActivity"
            android:exported="false" />

        <activity
            android:name="com.example.platemate.TrendingActivity"
            android:exported="false" />

        <activity
            android:name="com.example.platemate.VideoDetailActivity"
            android:exported="false" />

        <activity
            android:name="com.example.platemate.UploadActivity"
            android:exported="false" />

        <activity
            android:name="com.example.platemate.GuideListActivity"
            android:exported="false" />

    </application>

</manifest>